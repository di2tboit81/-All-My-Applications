{
  "name": "cek-kantong",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . cek-kantong --platform=win32 --arch=x64 --overwrite --icon=icon.ico"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.1"
  }
}